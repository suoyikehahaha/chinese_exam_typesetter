# 高中语文试卷排版 v0.4 界面语境

本文件补充 `CONTEXT.md`，记录 v0.4 已确认的界面语言。

**结构节点**：试卷结构树中的卷首、章节、材料、题目或答案区域。

**内容对象**：结构节点内部可以独立选择、编辑和定位的最小语义单元，例如题干、单个选项、材料段落、作者、注释或出处。

**属性检查器**：左侧随当前结构节点和内容对象变化的编辑区域，包含内容、字体、段落、选择项、分页和整卷设置。

**只读预览**：右侧由内部渲染器生成的连续 A4 页面。预览负责显示分页和定位，不承担文字编辑。

**批量范围**：将当前对象格式应用到当前题目、当前大题或整份试卷中的同类型对象。

**提醒**：分值缺失、总分差异、识别提示和题量差异会显示在黄色提示条中。提醒不会阻止导入，导出前允许用户继续导出或返回检查。
