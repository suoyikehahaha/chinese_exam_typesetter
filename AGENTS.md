# 项目目标

本项目把高中语文 Word 试题转换为规范、可编辑、可复现的 DOCX 文件，并提供结构化编辑和实时页面预览。

# 当前运行边界

1. 桌面端只接受 `.docx` 和 `.doc` 导入。
2. 桌面端只生成 `.docx`，不生成用户可见的 PDF。
3. DOCX 由程序直接读写，启动和日常编辑不要求 Office 或 WPS。
4. DOC 转换按需检测 Microsoft Word 和 WPS Office，两个引擎都必须保留兼容路径。
5. Word、WPS 和 PowerShell 调用必须隐藏窗口，启动阶段不得唤起办公软件。
6. 旧版模板文件只读，生成结果写入独立的 `output` 目录。
7. 表格、图片、着重号、下划线和其他原文字符装饰必须保留。

# 核心规则

1. 试题内容、结构数据、版式配置和渲染逻辑相互分离。
2. 正式输出统一由 `ExamDocument` 数据模型生成。
3. 字体、字号、缩进、行距、段距和页边距从配置读取。
4. 中文字体同时设置 `ascii`、`hAnsi`、`eastAsia` 和 `cs`。
5. 目标页数默认 8 页，只在安全范围内调节段落节奏，并显示实际预览页数。
6. 所有题型通过结构证据识别，不能把题号直接绑定为固定题型。
7. 材料标题、作者、出处、诗歌分行和作文三段式规则必须保持独立语义。

# 质量要求

1. 导出前执行结构、题号、分值、字体和文件完整性检查。
2. 预览修改必须可取消旧任务，避免旧结果覆盖新结果。
3. 异常信息需要说明失败对象和可执行的修复建议。
4. 不吞掉异常，不静默替换缺失字体。
5. 临时文件写入 `work` 或系统临时目录，并在任务结束后清理。
6. 修改渲染器、导入器或 Office/WPS 桥接层后运行全部测试。

# 项目文件与仓库卫生规则

始终保持本地项目目录和 GitHub 仓库整洁、清晰、可维护，避免无效文件、历史副本、临时文件和旧打包产物长期堆积。

1. 优先修改当前正式文件。不要为了保存历史版本创建 `xxx_old.py`、`xxx_new.py`、`xxx_v2.py`、`xxx_final.py`、`xxx_backup.py`、`xxx_fixed.py` 等重复文件。源码历史统一交给 Git 管理。
2. 每次较大修改完成后，扫描整个项目目录，检查并清理已废弃 Python 文件、重复源码、历史备份、临时测试文件、无用日志、缓存、`__pycache__`、`.pytest_cache`、`.ruff_cache`、`.mypy_cache`、临时 TXT、临时 Markdown、无长期价值的分析报告、失效说明文档、旧 EXE、旧安装包、旧 ZIP、旧 build、过期 dist、打包中间产物和其他可重新生成且没有保留价值的文件。
3. Markdown 文档控制数量。长期保留 `README.md`、`AGENTS.md`、`CHANGELOG.md`、正式开发说明、正式使用说明、架构说明和部署说明。一次性过程文档先合并有价值信息，再删除。
4. 生成新的正式打包版本后，确认新版本可以正常启动、核心功能正常、数据兼容，再删除失去用途的旧 EXE、安装包、压缩包和构建产物。
5. 本地项目目录与 GitHub 仓库遵守相同的整洁原则。仓库中及时删除废弃、重复或失效文件。
6. 将可自动重新生成的文件加入 `.gitignore`，重点覆盖 Python 缓存、测试缓存、临时日志、临时输出、本地缓存、构建中间目录、IDE 临时文件和操作系统临时文件。
7. `build`、`dist`、`release` 目录按用途管理。可重新生成的中间文件及时清理，发布目录原则上只保留当前正式版本。确有回滚需要时，额外保留最近一个稳定版本，更早版本通过正式发布归档。
8. 删除源码、配置、资源或文档前，检查代码引用、配置引用、打包脚本、入口程序、唯一有效信息、当前运行版本、用户数据和可重新生成性。无法确认安全性的文件暂时保留，并在最终报告中说明。
9. 因兼容性确需保留旧版本文件时，命名必须清晰、连续。避免使用 `new`、`final`、`final2`、`fixed` 等含义模糊的命名。
10. 多个文件长期并存且职责不同，优先按职责命名，例如 `docx_exporter.py` 和 `pdf_exporter.py`。
11. 一个功能尽量只保留一个当前权威实现。旧文件中有价值的代码先合并到当前正式实现，再删除旧文件。
12. 保持合理模块划分，不把所有代码塞进一个巨大文件。清理目标是减少无效文件、重复文件和历史垃圾。
13. 每次较大任务完成后执行 Repository Hygiene 检查，检查重复源码、历史版本、临时文件、无用 Markdown、旧打包文件、缓存、废弃模块、`.gitignore`、待提交内容和其他可安全删除内容。
14. 清理完成后重新验证启动、import、`ModuleNotFoundError`、路径、核心功能以及构建或打包状态。
15. 最终汇报简要说明删除文件、合并文件、保留旧版本及原因、`.gitignore` 调整、当前正式版本和验证结果。

长期原则：Git 保存源码历史，GitHub Releases 保存正式历史发布版本，源码目录保存当前有效状态，可重新生成的文件不长期保存，没有长期维护价值的文件及时删除。本地项目和 GitHub 仓库都保持整洁，每完成一个重要版本主动清理一次项目。
