# 0.2.0 实施状态

## 本轮已完成

- 当前桌面入口改为 Word 专用导入，支持 `.docx` 与 `.doc`。
- `.doc` 导入按需检测 Microsoft Word 和 WPS Office，转换过程隐藏窗口。
- DOCX 继续由程序直接生成，导出界面只保留 DOCX。
- 实时预览切换为内部 PNG 页面渲染，无 Office、WPS 或 LibreOffice 时也能启动和预览。
- 结构树定位、预览页面和目标页数信息继续保持同步。
- 增加目标页数设置，默认 8 页，在安全范围内调整段落节奏。
- 增加 Windows 原生拖放入口，支持把 `.docx` 或 `.doc` 拖到窗口。
- 保留表格、图片和原文字符装饰的 DOCX 原生对象保护流程。
- 导出文件名继续采用导入文件名并添加“（排版）”。
- 新增 0.2.0 Word 工作流测试。

## 已知边界

- 页面目标是排版节奏的自动调节值，最终页数仍受 Word、WPS、字体安装情况和母版内容影响，软件会显示实际预览页数。
- 旧版 `.doc` 的转换质量取决于本机 Word 或 WPS 对该文件的兼容情况。
- 扫描件 OCR、PDF 导入和 PDF 导出已从当前桌面工作流移除。

## 验收重点

1. 无 Office、无 WPS 的电脑可以启动、导入 DOCX、实时预览并导出 DOCX。
2. 仅安装 Word 时可以导入 DOC 并继续排版。
3. 仅安装 WPS 时可以导入 DOC 并继续排版。
4. 导出目录只出现 DOCX 文件，不生成 PDF。
