# v0.4.5 拖放修复

## 修复内容

- 使用 Windows `SetWindowSubclass` 接收资源管理器的 `WM_DROPFILES` 消息。
- 使用指针宽度正确的 `HANDLE` 读取 HDROP 数据，兼容当前 Python 运行环境。
- 原生回调只负责读取并排队文件路径，Tk 主线程定时消费队列，避免回调重入和界面卡死。
- Tk 客户区和顶层窗口同时注册拖放，覆盖窗口空白区、结构区和预览区。
- 保留 DOCX、DOC 文件过滤与既有导入流程。

## 验证结果

- 单元测试 96 项通过。
- 成品 EXE 启动自检通过，拖放注册状态为 true。
- 成品 EXE 收到真实格式的 Windows 拖放消息 1 次，路径入队 1 次。
- 六校二联试题通过拖放导入，识别第 1 至 23 题。
- 软件内置“GitHub 更新页面”，可查看最新发布说明和下载文件。
